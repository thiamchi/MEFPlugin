<UserControl x:Class="TwinCATServer.TwinCATUI.Workspace.Container.TwinCATSettingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TwinCATServer.TwinCATUI.Workspace.Container"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="5*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Column="0" Grid.Row="0"
               Content="NetID : " />
        <TextBox Grid.Column="1" Grid.Row="0"
                 x:Name="NetID" />

        <Label Grid.Column="0" Grid.Row="1"
               Content="Port : " />
        <TextBox Grid.Column="1" Grid.Row="1"
                 x:Name="Port" />

        <Button Grid.Column="1" Grid.Row="2" HorizontalAlignment="Right"
                Padding="5" Margin="5"
                Content="Update" x:Name="Update"/>

    </Grid>
</UserControl>
