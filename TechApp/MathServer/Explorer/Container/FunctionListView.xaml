<UserControl x:Class="MathServer.Explorer.Container.FunctionListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MathServer.Explorer.Container"
             xmlns:convert="clr-namespace:MathServer.Framework.Converter"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <convert:trueToVisible x:Key="trueToVisible" TrueValue="Visible" FalseValue="Hidden"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <DockPanel Grid.Row="0">
            <ToggleButton x:Name="Toggle" Content="{Binding Icon}">
            </ToggleButton>
            <Label Content="Keyboard"/>
        </DockPanel>
        <GroupBox Grid.Row="1" Visibility="{Binding Toggle, Converter={StaticResource trueToVisible}}">
            <StackPanel>
                <Label Content="this"/>
                <Label Content="that"/>
                <Label Content="this"/>
            </StackPanel>
        </GroupBox>
    </Grid>
</UserControl>
