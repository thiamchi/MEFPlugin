<UserControl x:Class="MathServer.MathUI.Screenspace.AlphaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MathServer.MathUI.Screenspace"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Row="0" Grid.ColumnSpan="2">
            <ContentControl x:Name="Menu"/>
        </Grid>

        <Grid Grid.Row="1" Grid.Column="0">
            <ContentControl x:Name="Explorer"/>
        </Grid>

        <Grid Grid.Row="1" Grid.Column="1">
            <ContentControl x:Name="Workspace"/>
        </Grid>
    </Grid>
</UserControl>
